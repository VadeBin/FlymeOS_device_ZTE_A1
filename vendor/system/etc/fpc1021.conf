#
# FPC Sensor Config
#

configver 1.0

###############################################################################

[1020]
# ------------------------------------------------------------------------------
crop_area.left = 0
crop_area.right = 159
crop_area.top = 0
crop_area.bottom = 159

# ------------------------------------------------------------------------------
#
# ------------------------------------------------------------------------------

#set pp.DATA to point to the file that contains the sensor package specific DATA configuration 
pp.DATA = "/etc/fpc1021w160.bin"

# ------------------------------------------------------------------------------
# algorithm configuration
# ------------------------------------------------------------------------------

[algo]

# security = "convenience", "standard", "secure", "high"
#security="secure"

# identify, max number of images
#upd.max_images = 20

# identify, template update enable, 1 or 0
#upd.enable = 1

# enroll max number of images
#enr.max_images = 10

# enroll coverage 0-100
#enr.image_coverage = 60

# enroll quality 0-100
#enr.image_quality = 50

# enroll, 0 < score <=1.0
#enr.min_coverage_score = 0.2

# enroll, 0 <= score <=1.0
#enr.init_coverage_score = 0.03
